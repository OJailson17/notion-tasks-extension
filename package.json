{
	"name": "client",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "tsc && vite build",
		"preview": "vite preview",
		"deploy": "yarn build && cp manifest.json dist/"
	},
	"devDependencies": {
		"typescript": "^4.6.4",
		"vite": "^3.1.0"
	}
}
